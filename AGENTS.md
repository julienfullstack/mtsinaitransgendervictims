# Agent instructions

Read these project documents in full on every request involving this project:

- `AGENTS.md` (this file)
- `DESIGN.md` — design and content rules
- `README.md` — structure and data model

Session task queues live in `/.ai-queues/` and are never committed.
